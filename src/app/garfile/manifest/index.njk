{% extends "base-template.njk" %}

{% import "common/templates/includes/macros.njk" as m %}

{% block pageTitle %}
	Create GAR | Manifest
{% endblock %}

{% block header %}
{% include "header-user.njk" %}
{% endblock %}

{% block beforeContent %}

  {% include "phase.njk" %}
  <a href="/garfile/craft" class="govuk-back-link">Back</a>

{% endblock %}

{% block content %}

  {% import "common/templates/includes/macros.njk" as m %}

  {% if errors %}
    {% include "common/templates/includes/errors.njk" %}
  {% endif %}

<div class="govuk-grid-row">
	<div class="govuk-grid-column-two-thirds">
		<form action="/garfile/manifest" method="post">
			<span class="govuk-caption-xl">Section 4 of 8</span>
			<h1 class="govuk-heading-xl">Manifest details</h1>

			<h2 class="govuk-heading-l">{{ __('nav_bar_people') }}</h2>
				<p class="govuk-body-l">Add the details of people you have previously added</p>
  </div>
  <div class="govuk-grid-column-full">
        {% if cookie.getSavedCraft().items | length %}
        <table class="table-clickable govuk-table">
        {% else %}
        <table class="govuk-table">
        {% endif %}
          <thead class="govuk-table__head">
          <tr class="govuk-table__row">
            <th style="width: 5%;" class="govuk-table__header multiple-choice--hod">
              {% if cookie.getSavedCraft().items | length %}
              <div class="govuk-checkboxes__item">
                <input type="checkbox" class="govuk-checkboxes__input jsCheckboxAll" id="radio-all">
                <label class="govuk-label govuk-checkboxes__label" for="radio-all">
                  &nbsp;
                </label>
              </div>
              {% endif %}
            </th>
            <th class="govuk-table__header">Surname</th>
            <th class="govuk-table__header">Given name</th>
            <th class="govuk-table__header">Type</th>
          </tr>
          </thead>
          <tbody class="govuk-table__body">
          {% if savedPeople | length %}
            {% for person in savedPeople %}
            <tr class="govuk-table__row">
              <td class="govuk-table__cell multiple-choice--hod">
                <div class="govuk-checkboxes__item">
                <input type="checkbox" class="govuk-checkboxes__input jsCheckbox" id="savedPerson-{{loop.index0}}" name="personId" value="{{person.personId}}">
                <label class="govuk-label govuk-checkboxes__label visuallyhidden" for="savedPerson-{{loop.index0}}">
                  &nbsp;
                </label>
                </div>
              </td>
              <td class="govuk-table__cell">{{person.lastName}}</td>
              <td class="govuk-table__cell">{{person.firstName}}</td>
              <td class="govuk-table__cell">{{person.peopleType.name}}</td>
            </tr>
            {% endfor %}
          {% else %}
            <tr class="govuk-table__row">
              <td colspan="4"><p class="govuk-inset-text">{{__('people_no_records')}}</p></td>
            </tr>
          {% endif %}
          </tbody>
        </table>
  </div>
  <div class="govuk-grid-column-two-thirds">
        <button type="submit" name="addToManifest"  class="govuk-button">Add to GAR</button>
  </div>
  <div class="govuk-grid-column-full">
        <hr class="govuk-section-break govuk-section-break--m govuk-section-break--visible">
  </div>
  <div class="govuk-grid-column-two-thirds">
				<h2 class="govuk-heading-l">New people</h2>
				<p class="govuk-body-m"><a href="/garfile/manifest/addnewperson">Add a new person to the GAR</a></p>
  </div>
  <div class="govuk-grid-column-full">
        <hr class="govuk-section-break govuk-section-break--m govuk-section-break--visible">
  </div>
  <div class="govuk-grid-column-two-thirds">
				<h2 class="govuk-heading-l">Current manifest</h2>
				<p class="govuk-body-l">People currently on the manifest of this GAR</p>
  </div>
  <div class="govuk-grid-column-full">
					<table class="govuk-table">
						<thead class="govuk-table__head">
							<tr class="govuk-table__row">
								<th class="govuk-table__header" scope="col">Surname</th>
								<th class="govuk-table__header" scope="col">Given name</th>
								<th class="govuk-table__header" scope="col">Date of birth</th>
								<th class="govuk-table__header" scope="col">Travel document number</th>
								<th class="govuk-table__header" scope="col">Nationality</th>
								<th class="govuk-table__header" scope="col">Type</th>
                <th class="govuk-table__header" scope="col">Action</th>
							</tr>
						</thead>
						<tbody class="govuk-table__body">
            {% if manifest.items | length %}
              {% for person in manifest.items %}
              {% if manifestInvalidPeople and (("person-" + loop.index0) in manifestInvalidPeople) %}
                <tr class="govuk-table__row row-error" id="person-{{loop.index0}}">
              {% else %}
                <tr class="govuk-table__row" id="person-{{loop.index0}}">
              {% endif %}

								<td class="govuk-table__cell">{{person.lastName}}</td>
								<td class="govuk-table__cell">{{person.firstName}}</td>
								<td class="govuk-table__cell">{{person.dateOfBirth}}</td>
								<td class="govuk-table__cell">{{person.documentNumber}}</td>
								<td class="govuk-table__cell">{{person.nationality | upper}}</td>
								<td class="govuk-table__cell">{{person.peopleType.name}}</td>
                <td class="govuk-table__cell">
                  <button class="link btn-link" name="editPersonId" value="{{person.garPeopleId}}">Edit</button>
                </td>
							</tr>
							<tr class="govuk-table__row">
								<td class="govuk-table__cell" colspan="7">
									<div>
										<details role="group">
											<summary role="button" aria-controls="details-content-2" aria-expanded="false" title="Further information for Chris Grover"><span class="summary">Further information</span></summary>
												<div class="panel panel-border-narrow" id="details-content-2" aria-hidden="true">
													<table class="govuk-table" width="100%">
														<tbody class="govuk-table__body">
															<tr class="govuk-table__row">
																<td class="govuk-table__cell">Gender</td>
																<td class="govuk-table__cell">{{person.gender}}</td>
															</tr>
															<tr class="govuk-table__row">
																<td class="govuk-table__cell">Place of birth</td>
																<td class="govuk-table__cell">{{person.placeOfBirth}}</td>
															</tr>
															<tr class="govuk-table__row">
																<td class="govuk-table__cell">Travel document type</td>
																<td class="govuk-table__cell">{{person.documentType}}</td>
															</tr>
															<tr class="govuk-table__row">
																<td class="govuk-table__cell">Travel document issuing state</td>
																<td class="govuk-table__cell">{{person.issuingState | upper }}</td>
															</tr>
															<tr class="govuk-table__row">
																<td class="govuk-table__cell">Travel document expiry date</td>
																<td class="govuk-table__cell">{{person.documentExpiryDate}}</td>
															</tr>
                              <tr class="govuk-table__row">
                                <td class="govuk-table__cell" colspan="7">
                                  <button class="link btn-link" name="deletePersonId" value="{{person.garPeopleId}}">Delete</button>
                                </td>
                              </tr>
														</tbody>
													</table>
												</div>
										</details>
									</div>
								</td>
							</tr>
              {% endfor %}
									</div>
								</td>
							</tr>
            {% else %}
              <tr class="govuk-table__row">
                <td colspan="4"><p class="govuk-inset-text">{{__('manifest_no_records')}}</p></td>
              </tr>
            {% endif %}
						</tbody>
					</table>
  </div>
  <div class="govuk-grid-column-two-thirds">
				<div class="govuk-form-group">
					<label class="govuk-label" for="totalPersonsOnBoard">
						Total persons on board
					</label>

					<input class="govuk-input govuk-input--width-3" id="totalPersonsOnBoard" name="name" type="number" value={{manifest.items.length}}>
				</div>

        <input class="govuk-button" type="submit" name="buttonClicked" value="Save and continue">
        <p class="govuk-body-m">
          <a href="/home" >Save and come back later</a>
        </p>
        <p class="govuk-body-m">
          <a href="/home" > Exit without saving </a>
        </p>
        <input type="hidden" name="_csrf" value="{{ csrfToken }}" />
		</form>
	</div>
</div>

{% endblock %}
